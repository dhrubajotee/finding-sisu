Theme cover photos aren't used yet (theme cards currently use an icon + color
instead), but this folder is here if you'd like to add theme banner images
later - just reference the path from data/themes.js and update ThemeCard.js.
